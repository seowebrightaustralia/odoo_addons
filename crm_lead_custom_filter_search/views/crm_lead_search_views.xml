<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Leads pipeline search filter -->
    <record id="view_crm_lead_search_sh_names_new" model="ir.ui.view">
        <field name="name">crm.lead.search.sh.names.new</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.view_crm_case_leads_filter"/>
        <field name="arch" type="xml">
            <!-- Broaden the default "Search for: X" (name field) to also match
                 partner name, sh_firstname, and sh_lastname so pressing Enter
                 finds leads by any of these fields automatically. -->
            <xpath expr="//field[@name='name']" position="replace">
                <field name="name"
                       filter_domain="['|', '|', '|', '|', '|', '|',
                           ('name', 'ilike', self),
                           ('partner_name', 'ilike', self),
                           ('partner_id.name', 'ilike', self),
                           ('sh_firstname', 'ilike', self),
                           ('sh_lastname', 'ilike', self),
                           ('description', 'ilike', self),
                           ('message_ids.body', 'ilike', self)]"/>
            </xpath>
            <xpath expr="//search" position="inside">
                <field name="sh_full_name_search" string="Search First or Last Name"/>
                <field name="sh_notes_search" string="Search Notes &amp; Messages"/>
            </xpath>
        </field>
    </record>

    <!-- Opportunities pipeline search filter -->
    <record id="view_crm_opportunity_search_sh_names" model="ir.ui.view">
        <field name="name">crm.lead.search.opportunity.sh.names</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.view_crm_case_opportunities_filter"/>
        <field name="arch" type="xml">
            <!-- Broaden the default "Search for: X" (name field). -->
            <xpath expr="//field[@name='name']" position="replace">
                <field name="name"
                       filter_domain="['|', '|', '|', '|', '|', '|',
                           ('name', 'ilike', self),
                           ('partner_name', 'ilike', self),
                           ('partner_id.name', 'ilike', self),
                           ('sh_firstname', 'ilike', self),
                           ('sh_lastname', 'ilike', self),
                           ('description', 'ilike', self),
                           ('message_ids.body', 'ilike', self)]"/>
            </xpath>
            <!-- Replace partner_id to add sh_firstname/sh_lastname branches
                 covering leads with no linked partner, and place
                 sh_full_name_search right after for explicit name search. -->
            <xpath expr="//field[@name='partner_id']" position="replace">
                <field name="partner_id" operator="child_of" string="Customer"
                       filter_domain="['|', '|', '|', '|', '|',
                           ('partner_id', 'ilike', self),
                           ('partner_name', 'ilike', self),
                           ('email_from', 'ilike', self),
                           ('contact_name', 'ilike', self),
                           ('sh_firstname', 'ilike', self),
                           ('sh_lastname', 'ilike', self)]"/>
                <field name="sh_full_name_search" string="Search First or Last Name"/>
                <field name="sh_notes_search" string="Search Notes &amp; Messages"/>
            </xpath>
        </field>
    </record>
</odoo>
